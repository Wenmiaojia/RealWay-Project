#RealWay project -- Research notes (March 9, 2026)

## Goals accomplished today
- Added WebSocket auto reconnect without breaking existing flows
- Added a Connection Status chip in the UI 
- ensure debounce is in plance to prevent duplicate POSTs
- keep the repo clean and journal updated

##BackEnd setup workFlow note
-open power shell in backEnd folder
  py -m uvicorn server:app --host 0.0.0.0 --port 8080 --reload

Key Code Changes (Summary)

MainScreen: added var wsState by remember { mutableStateOf("idle") } and chip; moved chip below header.
ButtonGroup: new param updateWsState: (String) -> Unit; forwarded it to click handlers.
onPark / onNavigate:

Added parameter updateWsState: (String) -> Unit.
Replaced old WS call with wsManager.connect(...).
Set onState = { s -> updateWsState(s) }.


Left the old connectWebSocket(...) function unused (safe to keep for now).